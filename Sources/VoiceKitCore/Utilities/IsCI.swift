//
//  IsCI.swift
//  VoiceKit
//
//  Generated by GPT-5 (OpenAI)
//  collaborator: rdoggett
//  date: 09-17-2025
//

import Foundation

/// Utility to detect CI environments for tests and live seams behavior.
/// By default, detects common CI via the `CI` environment variable.
/// You can override by setting `VOICEKIT_FORCE_CI=true` or `=false`.
public enum IsCI {
    public static var running: Bool {
        let env = ProcessInfo.processInfo.environment
        if let override = env["VOICEKIT_FORCE_CI"]?.lowercased() {
            return override == "1" || override == "true" || override == "yes"
        }
        if let ci = env["CI"]?.lowercased() {
            return ci == "1" || ci == "true" || ci == "yes"
        }
        return false
    }
}
