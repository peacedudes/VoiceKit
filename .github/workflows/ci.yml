name: CI

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  build-and-test:
    runs-on: macos-14
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Select Xcode (Swift 6)
        uses: maxim-lobanov/setup-xcode@v1
        with:
<<<<<<< HEAD
          xcode-version: '16.0'
=======
          xcode-version: '16.*'
>>>>>>> 33bce7e (Update .github ci.yml mostly)

      - name: Tool versions
        run: |
          xcodebuild -version
          swift --version

      - name: Resolve
        run: swift package resolve

      - name: Build (debug)
        run: swift build -v

      - name: Test
        run: swift test -v
